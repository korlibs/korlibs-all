sourceSets.main {
	kotlin.srcDir 'src'

	component {
		target nativeTargets
		outputKinds = [KLIBRARY]
	}
}
sourceSets.test {
	kotlin.srcDir 'test'
}

dependencies {
	expectedBy project(':' + project.name.replace("-native", "-common"))
	implementation project(":klock-native")
	implementation project(":kmem-native")
	implementation project(":kds-native")
	implementation project(":klogger-native")
}
